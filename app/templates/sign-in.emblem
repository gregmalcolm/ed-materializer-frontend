div.sign-in-panel
  h2 Sign in Commander
  
  form{action 'authenticate' on="submit"}
    label Email
    = input value=email type="text" placeholder="jameson@founders.net"
    label Password
    = input value=password type="password"
    .text-center
      button.button.text-center type="submit" 
        | Sign in
