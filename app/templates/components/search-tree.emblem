if systems
  ul
    each systems as |system|
      li
        = system.system
        ul
          each system.stars as |star|
            li
              = system.system
              '
              = star.star
          each system.surveying as |world|
            li
              = link-to 'surveying' (query-params systemName=system.system worldName=world.world)
                = system.system
                '
                = world.world
              '
              = world-survey-list worldSurvey=world.world_survey
else
  if isLoaded
    | No Systems found matching that criteria
  else
    | Searching...
